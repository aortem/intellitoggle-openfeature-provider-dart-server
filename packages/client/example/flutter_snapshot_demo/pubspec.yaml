name: intellitoggle_flutter_beta_demo
description: IntelliToggle snapshot-provider validation for the OpenFeature Dart client SDK beta.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  openfeature_provider_intellitoggle_client:
    path: ../..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
